<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Persona</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border: 1px solid black;
        }
        h1 {
            color: #333;
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
        }
        input[type="text"], input[type="number"], select {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
            box-sizing: border-box;
        }
        input[type="submit"] {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
        }
        input[type="submit"]:hover {
            background-color: #0056b3;
        }
        .home {
            margin-top: 20px;
            text-decoration: none;
            color: #fff;
            background-color: #333;
            padding: 10px 20px;
            border-radius: 5px;
            display: inline-block;
            margin-right: 20px;
        }
        .home:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <a href="/" style="display: block; margin-top: 20px;" class="home">Volver al Inicio</a>
    <div class="container">
        <h1>Editar Persona</h1>
        <form action="/edit/<%= persona.ID %>" method="POST">
            <label for="nombre">Nombre:</label>
            <input type="text" id="nombre" name="nombre" value="<%= persona.name %>"><br>
            <label for="apellido">Apellido:</label>
            <input type="text" id="apellido" name="apellido" value="<%= persona.lastname %>"><br>
            <label for="edad">Edad:</label>
            <input type="number" id="edad" name="edad" value="<%= persona.age %>"><br>
            <label for="barrio">Barrio:</label>
            <input type="text" id="barrio" name="barrio" value="<%= persona.barrio %>"><br>
            <label for="calle">Calle:</label>
            <input type="text" id="calle" name="calle" value="<%= persona.calle %>"><br>
            <label for="casa">Casa:</label>
            <input type="text" id="casa" name="casa" value="<%= persona.casa %>"><br>
            <label for="dengue">Dengue:</label>
            <select id="dengue" name="dengue">
                <option value="tipoA" <%= persona.dengue === 'tipoA' ? 'selected' : '' %>>Tipo A</option>
                <option value="tipoB" <%= persona.dengue === 'tipoB' ? 'selected' : '' %>>Tipo B</option>
                <option value="tipoC" <%= persona.dengue === 'tipoC' ? 'selected' : '' %>>Tipo C</option>
            </select><br>
            <button type="submit">Guardar Cambios</button>
        </form>
    </div>
    
</body>
</html>
